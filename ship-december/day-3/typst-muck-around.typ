Hello!

I would like this file to contain
some plain text
and also some typst

Codex says: Typst is a modern markup language for typesetting documents; it mixes text with lightweight layout commands (kind of like Markdown plus LaTeX features).

Here is a small Typst sample:

Some math: $e^(i pi) + 1 = 0$ 


